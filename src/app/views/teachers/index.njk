{% extends 'layout.njk' %}

{% block content %}

    <div class="card table-container">
        
        <table width="100%">
            <thead>
                <tr>
                    <th>teacher</th>
                    <th>Atendimento</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                {% for teacher in teachers %}
                    <tr>
                        <td><span style="background-image: url({{teacher.avatar_url}})" ></span></td>
                        <td>{{teacher.name}}</td>
                        <td>{{teacher.area_de_atuacao}}</td>
                        <td><a href="/teachers/{{teacher.id}}">ir</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="/teachers/create">Novo</a>
    </div>
    
{% endblock content %}